# Rewrite: servir site/ como raiz e bloquear arquivos sensiveis
<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteRule .* - [E=HTTP_AUTHORIZATION:%{HTTP:Authorization}]

    # Bloquear arquivos sensiveis (403 Forbidden)
    RewriteRule ^CLAUDE\.md$ - [F,L]
    RewriteRule ^\.gitignore$ - [F,L]
    RewriteRule ^docs(/.*)?$ - [F,L]
    RewriteRule ^\.github(/.*)?$ - [F,L]
    RewriteRule ^\.git(/.*)?$ - [F,L]
    RewriteRule \.md$ - [F,L]
    RewriteRule \.yml$ - [F,L]

    # Se ja esta acessando site/ diretamente, nao reescrever
    RewriteCond %{REQUEST_URI} ^/site/
    RewriteRule ^ - [L]

    # Reescrever tudo para site/
    RewriteRule ^(.*)$ site/$1 [L]
</IfModule>

# Cache de assets estaticos
<IfModule mod_expires.c>
    ExpiresActive On
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType text/css "access plus 1 month"
    ExpiresByType application/javascript "access plus 1 month"
</IfModule>

# Compressao GZIP
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/html text/css application/javascript text/xml application/json image/svg+xml
</IfModule>
